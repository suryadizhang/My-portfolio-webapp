# ========================================
# Portfolio Backend API - Environment Configuration
# ========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to git (it's in .gitignore)

# ========================================
# CRITICAL: OpenAI API Configuration
# ========================================
# Required for AI chat features
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1

# ========================================
# CRITICAL: CORS Origins
# ========================================
# Comma-separated list of allowed origins (no spaces after commas)
# Add all your frontend domains here
ALLOWED_ORIGINS=https://myportfolio.mysticdatanode.net,https://www.myportfolio.mysticdatanode.net,https://my-portfolio-webapp-web.vercel.app,https://suryadizhang.dev,http://localhost:3000

# ========================================
# OPTIONAL: Vercel KV (Analytics Storage)
# ========================================
# Get these from: Vercel Dashboard > Storage > KV > .env.local tab
# If not set, analytics will use in-memory fallback (data lost on restart)
# KV_REST_API_URL=https://your-kv-instance.kv.vercel-storage.com
# KV_REST_API_TOKEN=your-vercel-kv-token-here

# ========================================
# OPTIONAL: Resume Security
# ========================================
# Generate secure secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
# If not set, resume downloads won't require signed tokens
# RESUME_SIGNING_SECRET=your-random-32-char-secret-here
# SIGNED_DOWNLOADS=false

# ========================================
# Application Configuration
# ========================================
NODE_ENV=production
LOG_LEVEL=info
PORT=8000

# ========================================
# Quick Setup Guide for VPS Deployment
# ========================================
# 1. SSH into your VPS: ssh apiportfolio@108.175.12.154
# 2. Navigate to app directory: cd ~/python-service
# 3. Copy this file: cp .env.example .env
# 4. Edit with your values: nano .env
# 5. Save and restart service:
#    pkill -f uvicorn
#    source venv/bin/activate
#    nohup python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 > app.log 2>&1 &
# 6. Check health: curl http://localhost:8000/health
# 7. View logs: tail -f app.log
